"use client";

type CloseoutFlowProps = {
  jobId: string;
  boatName: string;
  categoryName: string;
  multiplier: number;
  startOpen?: boolean;
};

export default function CloseoutFlow(_props: CloseoutFlowProps) {
  return null;
}
